name: auto-tag-release-push
on:
  push:
    branches:
      - main
      - master

permissions:
  contents: write
  pull-requests: read

jobs:
  tag:
    uses: ./.github/workflows/auto-tag-release.yml
