name: probe-inner
on:
  workflow_call:
    inputs:
      msg:
        type: string
        default: "inner"
jobs:
  inner:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - run: echo "inner ran with ${{ inputs.msg }}"
