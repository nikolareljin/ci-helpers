version: 2
updates:
  - package-ecosystem: github-actions
    # "/" covers .github/workflows only; the composite actions under
    # .github/actions pin their own action SHAs and need listing too.
    directories:
      - /
      - /.github/actions/*
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
    open-pull-requests-limit: 10
    groups:
      github-actions:
        patterns: ["actions/*"]
